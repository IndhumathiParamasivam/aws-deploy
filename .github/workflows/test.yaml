name: test job
on:
  workflow_dispatch:
    inputs:
        version:
          description: 'Updated version'
          required: true
          default: 'v'
          type: string
jobs:
  test:
     runs-on: [ ubuntu-latest]
     steps:
      - name: test1
        run: |
         echo ${{ github.run_number }}
         var="v${{ github.run_number }}"
         var1="${{ inputs.version:1}}"  
         new=$((28-1))
         echo $new
         blue=$(($var1-1))
         echo "hhh-$blue"
