name: test job
on:
  push:
    branches: [ main ]
jobs:
  test:
     runs-on: [ ubuntu-latest]
     steps:
      - name: test1
        run: |
         echo ${{ github.run_number }}
         blue= `expr ${{ github.run_number }} - 1`
         echo "hhh-$blue"
